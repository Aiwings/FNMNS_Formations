<?php 
	
	$sql_user = "root";
	$sql_db = "fnmns-lorraine";
	$sql_pw = "23AiW1ngs";
	$sql_host = "localhost";

	$bdd = new PDO('mysql:host='.$sql_host.';dbname='.$sql_db.';charset=utf8', $sql_user , $sql_pw );
	
	
	$centres = [];
	
	$sql_select = "SELECT * FROM `centre_formation`";
	
	$reponse_select = $bdd->query($sql_select);	
	$reponse_select->setFetchMode(PDO::FETCH_ASSOC);
		

		if ($reponse_select->rowCount() >= 1) 
		{
			foreach ($reponse_select as $row)
				{
					$centres[] = $row;
				}
				echo json_encode($centres);
		}
	
	